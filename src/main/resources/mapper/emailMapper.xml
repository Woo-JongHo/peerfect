<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="email">
    <insert id="setVerify" parameterType="VerifyVO">
        INSERT INTO email (verify_email, verify_password, verify_expire)
        VALUES (#{verifyEmail}, #{verifyPassword}, #{verifyExpire})
    </insert>

    <select id="getVerify" parameterType="map" resultType="int">
        SELECT COUNT(*)
        FROM email_verification
        WHERE email_address = #{email} AND email_auth = #{authCode}
    </select>
</mapper>