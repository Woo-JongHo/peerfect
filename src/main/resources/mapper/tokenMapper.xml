<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="token">
    <insert id="saveToken">
        INSERT INTO token (member_id, access_token, refresh_token, expire_token)
        VALUES (#{memberId}, #{accessToken}, #{refreshToken}, #{expireToken})
    </insert>
</mapper>